metadata:
  short_description: "Self-contained, contract-first prompt and phase refinment for Codex"
  default_prompt: "Use $refinment only if this task or phase boundary materially needs a tighter contract, then show the refined prompt before proceeding and keep the method flexible unless the user fixed it."
  tags:
    - prompts
    - workflow
    - orchestration
  allow_implicit_invocation: true

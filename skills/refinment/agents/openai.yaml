metadata:
  short_description: "Self-contained refinement for ambiguous prompts, specs, and verification briefs"
  default_prompt: "Use $refinment only if this prompt or brief has a real contract gap, conflict, or ambiguous next step. Tighten it without over-constraining the method, skip routine phase transitions, and show a short Refined prompt block only when you actually refine."
  tags:
    - prompts
    - workflow
    - specs
    - verification
    - refinement
  allow_implicit_invocation: true

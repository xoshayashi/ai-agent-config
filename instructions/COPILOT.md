# GitHub Copilot CLI Global Instructions

This file is installed as `~/.copilot/copilot-instructions.md`.

Before starting any task, **read and follow `AI_AGENT_INSTRUCTIONS.md` in this same directory**.

For any user-facing output that touches tone, copy, color, typography, layout, or UI suggestions, also **read and follow `DESIGN.md`** in this same directory.

For Hook-driven self-workflow behavior and completion keywords, also **read and follow `HOOKS.md`** in this same directory.

If any of these files cannot be read, **stop and ask the user how to proceed**.
